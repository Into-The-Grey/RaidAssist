# .env.example — Rename to `.env` and fill in your real credentials

# Bungie API application key (never commit your real key!)
BUNGIE_API_KEY=your_bungie_api_key_here

# Bungie OAuth client ID
BUNGIE_CLIENT_ID=your_client_id_here